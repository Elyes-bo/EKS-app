apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-express-app.fullname" . }}-test-connection"
  labels:
    {{- include "my-express-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-express-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
